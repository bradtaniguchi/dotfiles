import { log } from 'console';

/**
 * Installs the vim and neovim configurations.
 *
 * @param {boolean} [dryRun] whether to run in dry mode, which will
 * prevent writes
 */
export async function installVimAndNvimConfigs(dryRun) {
  // TODO:
  log('Installing vim and neovim configs', { dryRun });
}
